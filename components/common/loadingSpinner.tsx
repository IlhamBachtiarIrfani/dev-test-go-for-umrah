import React from 'react'

// LoadingSpinner component to render a loading spinner
export default function LoadingSpinner() {
    {/* Render the loading spinner */ }
    return (
        <div className='loading-spinner' />
    )
}
